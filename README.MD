# sql-server-compose

A simple Docker Compose to run mssql in container with persistent volume. Customize at your will!

## How to use

1. Change MSSQL_SA_PASSWORD to a strong password (vide mssql rules)
2. Run `docker compose up -d` while inside the directory
3. Now you should be good to go